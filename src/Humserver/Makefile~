SUBDIRS =HumTrans Meters Humserver

all:	
	for %d in ($(SUBDIRS)) do \
	(cd %d && nmake all && cd ..) 

run:
	cd bin && Humserver.exe

clean:
	for %d in ($(SUBDIRS)) do \
	(cd %d && nmake clean && cd ..)


